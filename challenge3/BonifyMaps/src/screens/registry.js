/** 
 * The order of imports matter, since we are using a stack navigator.
 *  The first screen imported will be the root screen. Also sacrificed 
 * using jsx so this could be a tad bit easier to the eye.
*/
export { MapsIndexScreen } from 'src/screens/maps/index';
export { VideosIndexScreen } from 'src/screens/videos/index/'
export { VideosShowScreen } from 'src/screens/videos/show/'
