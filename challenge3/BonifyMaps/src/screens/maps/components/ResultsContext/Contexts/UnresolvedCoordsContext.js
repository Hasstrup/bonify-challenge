import React from 'react';
import { View, Text } from 'react-native';

const UnresolvedCoordsContext = (props) => (
    <View> 
        <Text>Hello there from Unresolved coords context</Text>
    </View>
)

export default UnresolvedCoordsContext