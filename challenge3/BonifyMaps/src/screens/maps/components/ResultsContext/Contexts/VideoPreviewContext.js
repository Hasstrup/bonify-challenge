import React from 'react';
import { View, Text } from 'react-native';

const VideoPreviewContext = (props) => (
    <View> 
        <Text>Hello there from video preview</Text>
    </View>
)

export default VideoPreviewContext