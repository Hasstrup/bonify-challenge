import React from 'react';
import { View, Text } from 'react-native';

const NewUserContext = (props) => (
    <View> 
        <Text>Hello there from New User context</Text>
    </View>
)

export default NewUserContext